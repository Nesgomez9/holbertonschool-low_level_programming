#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 * @argv: string
 * @argc: int
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	if (argc >= 0)
		printf("%s\n", argv[0]);
	return (0);
}
